spring.application.name=smartmenu

# Configura��es do PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/smart_menu
spring.datasource.username=muser
spring.datasource.password=mpassword
spring.datasource.driverClassName=org.postgresql.Driver

# Dialeto espec�fico para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Para gerar o schema automaticamente (�til durante o desenvolvimento)
spring.jpa.hibernate.ddl-auto=update

# Outras configura��es do JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Jwt Settings
jwt.secret-key=my-secret-key
jwt.expiration-time=12
